import React from "react";
import ReactDOM from "react-dom/client";
import { HDLSApp } from "./HDLSApp";
import "./HDLSApp.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <HDLSApp />
  </React.StrictMode>
);
